---
const { title, description, date, slug} = Astro.props
---

<a href={`/blog/${slug}`} class="flex flex-col gap-4 w-full h-full border border-borderColor rounded-sm hover:border-terciary p-4 cursor-pointer">
	<h3 class="text-white text-xl font-semibold">
		{title}
	</h3>
	<p class="text-slate-400">
		{description}
	</p>
	<p class="w-full flex flex-row justify-end text-white">
		{date}
	</p>
</a>
